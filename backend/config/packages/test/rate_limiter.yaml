# Disable rate limiting in tests
framework:
    rate_limiter:
        registration:
            policy: 'no_limit'
        group_join:
            policy: 'no_limit'
        password_reset:
            policy: 'no_limit'
